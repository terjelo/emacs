(setq default-frame-alist
      '((top . 0) (left . 50)
	(width . 82) (height . 47)
	(cursor-color . "yellow")
	))

(setq load-path
      (nconc '( "~/emacs/site-lisp/testsuite")
      	     '( "~/emacs/site-lisp/autotest.el")
             '( "~/emacs/site-lisp")
             '( "~/emacs/site-lisp/ri-emacs-0.2.3")
             load-path))

;; (setq have-python t)
;; (setq have-backup-dir t)
(setq have-markdown t)
(setq have-rubyblock t)
(setq have-browse-kill-ring t)
(setq have-psvn 't)
;;(setq have-autotest t)
